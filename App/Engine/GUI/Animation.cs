using System;
using System.Collections.Generic;
using System.Text;

namespace WtfApp.GUI
{
    public enum AnimationType : int { TO_BIG_TO_DEFAULT, MOVE_TO, t3 };

    public class Animation
    {
        bool isActive;

        
       

       /* public static void Animate(GuiObject guiObject, AnimationType animationType, int? offsetX = 0, int? offsetY = 0, float? scaleX = 0f, float? scaleY = 0f)
        {
            Animate(null, AnimationType.MOVE_TO,offsetX:1);
            //param.val1  
        }*/

    }
}
